# Sample Data

You can place a CSV file here for custom experiments.

Expected columns:

- `Date`
- `Close`

If you do not supply a dataset path, `src/train.py` generates a synthetic demo series automatically.

